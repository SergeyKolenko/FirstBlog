var like_div =  $('#<%= params[:resource_name].downcase%>_<%= params[:resource_id] %> .pull-right');
$(like_div).find('span').text('<%= @target.like_count %>');
$(like_div).find('a').replaceWith('<%= like_link_for(@target) %>');